export type BlogTypes = {
  img: {
    id: string;
    url: string;
    blurUpThumb: string;
  };
};

export type PortfolioItem = {
  prevPrice: string;
  price: string;
  href: string;
  url: string;
  label: string;
  desc: string;
}